#include <stdio.h>

/**
 * main -  Print all possible combinations of single-digit
 * Return: 0
 */

int main(void)
{
int combo;

for (combo = 48; combo < 58; combo++)
{
putchar(combo);
if (combo != 57)
{
putchar(,);
putchar( );
}
}
putchar(n);
return (0);
}
